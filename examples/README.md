# API examples

Start the backend first, then run the examples from the repository root:

```bash
python examples/chat.py "Summarize the Tiff architecture"
python examples/execute.py code
```

Both scripts use Python's standard library and target `http://localhost:8000`.
